import {
  TextField as TF
} from '@radix-ui/themes'

const TextField = TF.Root
const TextFieldSlot = TF.Slot
const TextFieldInput = TF.Input

export {
  TextField,
  TextFieldSlot,
  TextFieldInput
}